# ---- Core web app ----
Flask>=3.0
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3

# ---- Full MalFusion model (recommended) ----
# These give the complete ensemble + SMOTE balancing and the higher accuracy
# reported in the research notebook. If any of them fail to install on your
# machine, the app still runs using built-in scikit-learn fallbacks.
imbalanced-learn>=0.11
xgboost>=2.0
catboost>=1.2
